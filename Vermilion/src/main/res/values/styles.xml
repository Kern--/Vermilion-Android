<resources>

    <!-- Base application theme. -->
    <style name="Custom" parent="Theme.AppCompat.Light">
        <item name="actionBarTabStyle">@style/Custom.ActionBar.TabView.Empty</item>
        <item name="actionBarStyle">@style/Custom.ActionBar.Empty</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="Custom.ActionBar.Empty" parent="@style/Widget.AppCompat.Light.ActionBar">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="Custom.ActionBar.TabView.Empty" parent="@style/Widget.AppCompat.ActionBar.TabView">
        <item name="android:background">@null</item>
        <item name="android:padding">0dp</item>
    </style>

</resources>
